В программе не корректно обозначен диапазон возраста тинэйджеров(<19).
Поэтому тест на возрасте 19 лет выдает False.
Также и возраст 0, и 10 лет не являются возрастом тинэйджеров, а в программе не отмечены, и тест падает.
Возраст должен быть обозначен от 13 включительно и до 19 включительно(>= 13 && age <= 19).
public class Person {
    public static boolean isTeenager(int age)
    {
        boolean result;
        if (age >= 13 && age <= 19) {
            result = true;
        }
        else {
            result = false;
        }
        return result;
    }
}
